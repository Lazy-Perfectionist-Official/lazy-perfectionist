'use client';

import { useState, useEffect, useRef } from 'react';
import Link from 'next/link';
import {
  Youtube,
  Music,
  BookOpen,
  ExternalLink,
  ChevronDown,
  Play,
  Menu,
  X,
  Sparkles,
  Zap,
  ArrowRight,
  Calendar,
  MapPin,
} from 'lucide-react';
import { motion, useScroll, useInView, useTransform, useSpring } from 'framer-motion';
import ArtistTimeline from '@/components/ArtistTimeline';

// ---------------------------------------------------------------------
// YouTube ID extraction
const getYouTubeId = (url: string) => {
  const regExp =
    /^.*(youtu.be\/|v\/|u\/\w\/|embed\/|watch\?v=|&v=)([^#&?]*).*/;
  const match = url.match(regExp);
  return match && match[2].length === 11 ? match[2] : null;
};

const youtubeId = getYouTubeId('https://www.youtube.com/watch?v=Hw2a43RV1p0');

// ---------------------------------------------------------------------
// Custom hook for magnetic hover effect
const useMagneticHover = () => {
  const ref = useRef<HTMLDivElement>(null);
  const [position, setPosition] = useState({ x: 0, y: 0 });

  const handleMouseMove = (e: React.MouseEvent) => {
    if (!ref.current) return;

    const rect = ref.current.getBoundingClientRect();
    const centerX = rect.left + rect.width / 2;
    const centerY = rect.top + rect.height / 2;

    const x = (e.clientX - centerX) * 0.15;
    const y = (e.clientY - centerY) * 0.15;

    setPosition({ x, y });
  };

  const handleMouseLeave = () => {
    setPosition({ x: 0, y: 0 });
  };

  return {
    ref,
    style: {
      transform: `translate(${position.x}px, ${position.y}px)`,
      transition: 'transform 0.3s cubic-bezier(0.4, 0, 0.2, 1)',
    },
    onMouseMove: handleMouseMove,
    onMouseLeave: handleMouseLeave,
  };
};

// ---------------------------------------------------------------------
// Main component
export default function Home() {
  const [isMenuOpen, setIsMenuOpen] = useState(false);
  const [reduceMotion, setReduceMotion] = useState(false);

  // Enhanced scroll hooks for physics-based animations
  const { scrollY, scrollYProgress } = useScroll();
  const smoothScrollY = useSpring(scrollY, { stiffness: 400, damping: 40 });

  // Multiple scroll-based transforms for layered parallax
  const heroOpacity = useTransform(scrollY, [0, 400], [1, 0.3]);
  const heroScale = useTransform(scrollY, [0, 400], [1, 1.1]);
  const heroRotate = useTransform(scrollY, [0, 400], [0, 2]);

  const feature1Y = useTransform(scrollY, [0, 600], [0, -50]);
  const feature2Y = useTransform(scrollY, [0, 600], [0, -100]);
  const feature3Y = useTransform(scrollY, [0, 600], [0, -150]);

  // Diagonal section transforms
  const diagonalRotate = useTransform(scrollY, [0, 800], [0, -3]);

  // Hero overlay opacity that darkens on scroll
  const overlayOpacity = useTransform(scrollY, [0, 400], [0.4, 0.7]);

  // -----------------------------------------------------------------
  // Reduce-motion detection
  useEffect(() => {
    const mq = window.matchMedia('(prefers-reduced-motion: reduce)');
    setReduceMotion(mq.matches);
    const handler = (e: MediaQueryListEvent) => setReduceMotion(e.matches);
    mq.addEventListener('change', handler);
    return () => mq.removeEventListener('change', handler);
  }, []);

  // -----------------------------------------------------------------
  // Intersection observers for staggered animations
  const heroRef = useRef<HTMLDivElement>(null);
  const featureRef = useRef<HTMLDivElement>(null);
  const aboutRef = useRef<HTMLDivElement>(null);
  const videoRef = useRef<HTMLDivElement>(null);

  const heroInView = useInView(heroRef, { once: false, margin: '-100px' });
  const featureInView = useInView(featureRef, { once: true, margin: '-150px' });
  const aboutInView = useInView(aboutRef, { once: true, margin: '-150px' });
  const videoInView = useInView(videoRef, { once: true, margin: '-150px' });

  const magneticHover = useMagneticHover();

  return (
    <div className="min-h-screen linktree-gradient relative overflow-hidden">
      {/* -----------------------------------------------------------------
          Enhanced background with animated gradients */}
      <div className="fixed inset-0 -z-20">
        {/* Base gradient */}
        <div className="absolute inset-0 bg-gradient-to-br from-indigo-900/20 via-purple-800/30 to-indigo-700/25" />

        {/* Animated gradient orbs with physics */}
        <motion.div
          className="absolute top-20 left-10 w-96 h-96 bg-gradient-to-br from-indigo-500/30 to-transparent rounded-full blur-3xl"
          animate={!reduceMotion ? {
            x: [0, 100, 0],
            y: [0, -80, 0],
            scale: [1, 1.2, 1],
          } : {}}
          transition={{ duration: 12, repeat: Infinity, ease: 'easeInOut' }}
        />
        <motion.div
          className="absolute bottom-20 right-10 w-[500px] h-[500px] bg-gradient-to-tr from-purple-500/20 to-transparent rounded-full blur-3xl"
          animate={!reduceMotion ? {
            x: [0, -120, 0],
            y: [0, 100, 0],
            scale: [1, 0.8, 1],
          } : {}}
          transition={{ duration: 15, repeat: Infinity, ease: 'easeInOut', delay: 3 }}
        />
        <motion.div
          className="absolute top-1/2 left-1/3 w-80 h-80 bg-gradient-to-br from-indigo-500/15 via-transparent to-purple-400/20 rounded-full blur-2xl"
          animate={!reduceMotion ? {
            scale: [1, 1.4, 1],
            opacity: [0.3, 0.6, 0.3],
          } : {}}
          transition={{ duration: 8, repeat: Infinity, ease: 'easeInOut', delay: 1 }}
        />
      </div>

      {/* -----------------------------------------------------------------
          Multi-layered brilliant noise texture overlay */}
      <div
        className="fixed inset-0 pointer-events-none z-0"
        style={{
          backgroundImage: `url("/assets/img/noise.jpg")`,
          backgroundSize: '600px 600px',
          backgroundPosition: '0 0',
          backgroundRepeat: 'repeat',
          opacity: 0.15,
          mixBlendMode: 'soft-light',
          transform: 'scale(1.5)',
        }}
      />
      <div
        className="fixed inset-0 pointer-events-none z-0"
        style={{
          backgroundImage: `url("/assets/img/noise.jpg")`,
          backgroundSize: '200px 200px',
          backgroundPosition: '100px 100px',
          backgroundRepeat: 'repeat',
          opacity: 0.12,
          mixBlendMode: 'overlay',
          transform: 'scale(1.2) rotate(1deg)',
        }}
      />
      <div
        className="fixed inset-0 pointer-events-none z-0"
        style={{
          backgroundImage: `url("/assets/img/noise.jpg")`,
          backgroundSize: '100px 100px',
          backgroundPosition: '50px 25px',
          backgroundRepeat: 'repeat',
          opacity: 0.08,
          mixBlendMode: 'multiply',
          transform: 'scale(1.8) rotate(-0.5deg)',
        }}
      />

      {/* -----------------------------------------------------------------
          Enhanced Navigation with magnetic hover */}
      <motion.nav
        className="fixed top-4 left-4 right-4 z-50"
        initial={{ y: -100 }}
        animate={{ y: 0 }}
        transition={{ duration: 0.8, ease: 'easeOut' }}
      >
        <div className="max-w-7xl mx-auto bg-white/95 backdrop-blur-xl border-2 border-black rounded-3xl shadow-2xl overflow-hidden">
          <div className="flex items-center justify-between h-16 px-6">
            <motion.div
              className="flex items-center"
              whileHover={{ scale: 1.05 }}
              transition={{ type: 'spring', stiffness: 400 }}
            >
              <Link href="/" className="flex items-center">
                <span className="text-black font-bold text-xl font-dm-serif tracking-tight">
                  ✨ Lazy Perfectionist
                </span>
              </Link>
            </motion.div>

            <div className="hidden md:flex items-center space-x-8">
              {['Home', 'Music', 'Blog'].map((item, i) => (
                <motion.div key={item} whileHover={{ scale: 1.1 }} whileTap={{ scale: 0.95 }}>
                  <Link
                    href={item === 'Home' ? '/' : `/${item.toLowerCase()}`}
                    className={`text-black hover:text-blue-600 transition-colors font-medium ${
                      item === 'Home' ? 'text-black' : 'text-black/70'
                    }`}
                  >
                    {item}
                  </Link>
                </motion.div>
              ))}
              <motion.a
                href="https://linktr.ee/lazyperfectionist_official"
                target="_blank"
                rel="noopener noreferrer"
                className="text-black/70 hover:text-blue-600 transition-colors font-medium flex items-center gap-1"
                whileHover={{ scale: 1.1 }}
                whileTap={{ scale: 0.95 }}
              >
                Links <ExternalLink size={14} />
              </motion.a>
            </div>

            <motion.button
              onClick={() => setIsMenuOpen(!isMenuOpen)}
              className="md:hidden text-black/70 p-2"
              whileHover={{ scale: 1.1 }}
              whileTap={{ scale: 0.95 }}
              aria-label="Toggle menu"
            >
              {isMenuOpen ? <X size={24} /> : <Menu size={24} />}
            </motion.button>
          </div>

          {/* Mobile menu with enhanced animations */}
          <motion.div
            initial={{ height: 0, opacity: 0 }}
            animate={{
              height: isMenuOpen ? 'auto' : 0,
              opacity: isMenuOpen ? 1 : 0
            }}
            transition={{ duration: 0.3, ease: 'easeOut' }}
            className="md:hidden overflow-hidden border-t border-black/20"
          >
            <div className="px-6 py-4 space-y-3">
              {['Home', 'Music', 'Blog'].map((item, i) => (
                <motion.div key={item} whileHover={{ x: 10 }}>
                  <Link
                    href={item === 'Home' ? '/' : `/${item.toLowerCase()}`}
                    className={`block text-black hover:text-blue-600 transition-colors font-medium py-2 ${
                      item === 'Home' ? 'text-black' : 'text-black/70'
                    }`}
                  >
                    {item}
                  </Link>
                </motion.div>
              ))}
              <motion.a
                href="https://linktr.ee/lazyperfectionist_official"
                target="_blank"
                rel="noopener noreferrer"
                className="text-black/70 hover:text-blue-600 transition-colors font-medium flex items-center gap-1 py-2"
                whileHover={{ x: 10 }}
              >
                Links <ExternalLink size={14} />
              </motion.a>
            </div>
          </motion.div>
        </div>
      </motion.nav>

      {/* -----------------------------------------------------------------
          HERO – Image centered, gradient darkens on scroll */}
      <section className="min-h-screen flex items-center justify-center bg-black text-white pt-20">
        <div className="text-center px-4 sm:px-6 lg:px-8 max-w-4xl">
          <h1 className="text-6xl md:text-8xl font-bold mb-6">
            @Lazy Perfectionist
          </h1>

          <p className="text-2xl md:text-3xl mb-12">
            Bedroom Instrumental Rock!
          </p>

          {/* Enhanced CTA buttons with staggered entrance */}
          <motion.div
            className="flex flex-col sm:flex-row gap-6 justify-center mb-12"
            initial={{ y: 30, opacity: 0 }}
            animate={heroInView ? { y: 0, opacity: 1 } : {}}
            transition={{ duration: 0.8, delay: 1, ease: 'easeOut' }}
          >
            <motion.a
              href="https://open.spotify.com/track/1XIv8JGEDU9MZT6HEFmdk8"
              target="_blank"
              rel="noopener noreferrer"
              className="linktree-button inline-flex items-center justify-center px-8 py-4 font-semibold relative overflow-hidden group shadow-lg"
              whileHover={{
                scale: 1.05,
                y: -3,
                boxShadow: '0 20px 25px -5px rgba(34, 197, 94, 0.3)'
              }}
              whileTap={{ scale: 0.95 }}
              initial={{ x: -50, opacity: 0 }}
              animate={{ x: 0, opacity: 1 }}
              transition={{ delay: 1.1, duration: 0.6 }}
            >
              <motion.div
                className="absolute inset-0 bg-gradient-to-r from-green-500/20 to-emerald-500/20"
                initial={{ x: '-100%' }}
                whileHover={{ x: '0%' }}
                transition={{ duration: 0.3 }}
              />
              <motion.div
                className="relative z-10 flex items-center"
                whileHover={{ x: 5 }}
              >
                <Play className="mr-2" size={22} />
                Stream "Orbit" Now
              </motion.div>
            </motion.a>

            <motion.a
              href="https://medium.com/@lazyperfectist"
              target="_blank"
              rel="noopener noreferrer"
              className="linktree-button inline-flex items-center justify-center px-8 py-4 font-semibold relative overflow-hidden group shadow-lg"
              whileHover={{
                scale: 1.05,
                y: -3,
                boxShadow: '0 20px 25px -5px rgba(59, 130, 246, 0.3)'
              }}
              whileTap={{ scale: 0.95 }}
              initial={{ x: 50, opacity: 0 }}
              animate={{ x: 0, opacity: 1 }}
              transition={{ delay: 1.2, duration: 0.6 }}
            >
              <motion.div
                className="absolute inset-0 bg-gradient-to-r from-blue-500/20 to-indigo-500/20"
                initial={{ x: '-100%' }}
                whileHover={{ x: '0%' }}
                transition={{ duration: 0.3 }}
              />
              <motion.div
                className="relative z-10 flex items-center"
                whileHover={{ x: 5 }}
              >
                <BookOpen className="mr-2" size={22} />
                Read on Medium
              </motion.div>
            </motion.a>
          </motion.div>

          {/* Enhanced location info with icons */}
          <motion.div
            className="flex items-center justify-center gap-6 text-white/80"
            initial={{ opacity: 0, scale: 0.9 }}
            animate={heroInView ? { opacity: 1, scale: 1 } : {}}
            transition={{ duration: 0.8, delay: 1.4, ease: 'easeOut' }}
          >
            <motion.span
              className="flex items-center gap-2"
              whileHover={{ scale: 1.1, color: '#fbbf24' }}
            >
              <Music className="w-4 h-4" />
              Hong Kong
            </motion.span>
            <motion.span
              animate={!reduceMotion ? { opacity: [0.5, 1, 0.5] } : {}}
              transition={{ duration: 2, repeat: Infinity }}
            >
              •
            </motion.span>
            <motion.span
              className="flex items-center gap-2"
              whileHover={{ scale: 1.1, color: '#fbbf24' }}
            >
              <Zap className="w-4 h-4" />
              Lazy Perfectionist
            </motion.span>
          </motion.div>
          </div>
        </div>

        {!reduceMotion && (
          <motion.div
            className="absolute bottom-8 left-1/2 -translate-x-1/2"
            animate={{ y: [0, 10, 0] }}
            transition={{ duration: 1.5, repeat: Infinity, ease: 'easeInOut' }}
          >
            <ChevronDown className="w-8 h-8 text-white/60" />
          </motion.div>
        )}
      </section>

      {/* -----------------------------------------------------------------
          Diagonal separator */}
      <div className="relative h-32 -my-16">
        <motion.div
          className="absolute inset-0 bg-gradient-to-br from-transparent via-indigo-500/5 to-transparent"
          style={{ transform: 'skewY(-3deg)' }}
          initial={{ opacity: 0 }}
          whileInView={{ opacity: 1 }}
          transition={{ duration: 1 }}
          viewport={{ once: true }}
        />
      </div>

      {/* -----------------------------------------------------------------
          Video Section with Asymmetrical Layout */}
      <section
        ref={videoRef}
        className="relative py-24 px-6 sm:px-8 lg:px-12 overflow-hidden"
      >
        <div className="max-w-7xl mx-auto">
          <div className="grid lg:grid-cols-12 gap-12 items-center">
            {/* Content left */}
            <motion.div
              className="lg:col-span-5"
              initial={{ x: -50, opacity: 0 }}
              animate={videoInView ? { x: 0, opacity: 1 } : {}}
              transition={{ duration: 0.8, ease: 'easeOut' }}
            >
              <div className="relative">
                <motion.div
                  className="absolute -top-4 -left-4 w-24 h-24 bg-gradient-to-br from-indigo-500/20 to-purple-500/20 rounded-full blur-2xl"
                  animate={!reduceMotion ? { scale: [1, 1.2, 1] } : {}}
                  transition={{ duration: 4, repeat: Infinity, ease: 'easeInOut' }}
                />

                <h2 className="text-4xl md:text-5xl font-bold text-white mb-6 font-dm-serif relative">
                  Visual
                  <span className="block bg-gradient-to-r from-indigo-300 to-purple-300 bg-clip-text text-transparent">Experience</span>
                </h2>

                <p className="text-xl text-white/80 mb-8 leading-relaxed">
                  Watch the official video for "Orbit" — a visual journey through the contrasts that define the Lazy Perfectionist sound.
                </p>

                <div className="space-y-4">
                  {[
                    { icon: Youtube, text: 'Official Music Video', highlight: true },
                    { icon: Music, text: 'Available on all platforms', highlight: false },
                    { icon: Calendar, text: 'Released October 2025', highlight: false },
                  ].map((item, i) => (
                    <motion.div
                      key={i}
                      className="flex items-center gap-4"
                      initial={{ x: -20, opacity: 0 }}
                      animate={videoInView ? { x: 0, opacity: 1 } : {}}
                      transition={{ delay: 0.3 + i * 0.1, ease: 'easeOut' }}
                    >
                      <div className={`p-3 rounded-xl ${item.highlight ? 'bg-indigo-500/20 text-indigo-300' : 'bg-white/10 text-white/70'}`}>
                        <item.icon className="w-5 h-5" />
                      </div>
                      <span className="text-white font-medium">{item.text}</span>
                    </motion.div>
                  ))}
                </div>
              </div>
            </motion.div>

            {/* Video right - larger column */}
            <motion.div
              className="lg:col-span-7"
              initial={{ x: 50, opacity: 0 }}
              animate={videoInView ? { x: 0, opacity: 1 } : {}}
              transition={{ duration: 0.8, delay: 0.2, ease: 'easeOut' }}
            >
              <div className="relative aspect-video rounded-3xl overflow-hidden shadow-2xl">
                <div className="absolute inset-0 bg-gradient-to-r from-indigo-500/20 to-purple-500/20 z-10" />
                {videoInView && (
                  <iframe
                    className="w-full h-full"
                    src={`https://www.youtube.com/embed/${youtubeId}`}
                    title="Lazy Perfectionist - Orbit (Official Video)"
                    frameBorder="0"
                    allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
                    allowFullScreen
                    loading="lazy"
                  />
                )}
              </div>

              <motion.div
                className="flex flex-col sm:flex-row gap-4 mt-8"
                initial={{ y: 30, opacity: 0 }}
                animate={videoInView ? { y: 0, opacity: 1 } : {}}
                transition={{ duration: 0.6, delay: 0.6, ease: 'easeOut' }}
              >
                <motion.a
                  href={`https://www.youtube.com/watch?v=${youtubeId}`}
                  target="_blank"
                  rel="noopener noreferrer"
                  className="flex-1 linktree-button px-6 py-3 rounded-2xl font-semibold flex items-center justify-center gap-2"
                  whileHover={{ scale: 1.05 }}
                  whileTap={{ scale: 0.95 }}
                >
                  <Youtube className="w-5 h-5" />
                  Watch on YouTube
                </motion.a>
                <motion.a
                  href="https://open.spotify.com/track/1XIv8JGEDU9MZT6HEFmdk8"
                  target="_blank"
                  rel="noopener noreferrer"
                  className="flex-1 linktree-button px-6 py-3 rounded-2xl font-semibold flex items-center justify-center gap-2"
                  whileHover={{ scale: 1.05 }}
                  whileTap={{ scale: 0.95 }}
                >
                  <Music className="w-5 h-5" />
                  Stream Music
                </motion.a>
              </motion.div>
            </motion.div>
          </div>
        </div>
      </section>

      {/* -----------------------------------------------------------------
          About Section with Overlapping Cards */}
      <section
        ref={aboutRef}
        className="relative py-24 px-6 sm:px-8 lg:px-12"
      >
        <div className="max-w-7xl mx-auto">
          <motion.div
            className="text-center mb-16"
            initial={{ y: 30, opacity: 0 }}
            animate={aboutInView ? { y: 0, opacity: 1 } : {}}
            transition={{ duration: 0.8, ease: 'easeOut' }}
          >
            <h2 className="text-4xl md:text-5xl font-bold text-white mb-6 font-dm-serif">
              The Art of
              <span className="block bg-gradient-to-r from-indigo-400 via-purple-300 to-indigo-500 bg-clip-text text-transparent animate-pulse">Lazy Perfectionism</span>
            </h2>
            <p className="text-xl text-white/80 max-w-3xl mx-auto">
              Where bedroom creativity meets professional precision, creating instrumental rock that moves the soul.
            </p>
          </motion.div>

          {/* Overlapping cards layout */}
          <div className="relative">
            {/* Background cards */}
            <motion.div
              className="absolute top-8 right-8 w-full h-full bg-white/95 backdrop-blur-md rounded-3xl border-2 border-black shadow-2xl"
              initial={{ rotate: 1, scale: 0.95 }}
              animate={aboutInView ? { rotate: 0, scale: 1 } : {}}
              transition={{ duration: 0.8, delay: 0.2, ease: 'easeOut' }}
            />
            <motion.div
              className="absolute top-4 right-4 w-full h-full bg-white/90 backdrop-blur-md rounded-3xl border-2 border-black shadow-2xl"
              initial={{ rotate: -0.5, scale: 0.97 }}
              animate={aboutInView ? { rotate: 0, scale: 1 } : {}}
              transition={{ duration: 0.8, delay: 0.3, ease: 'easeOut' }}
            />

            {/* Main content card */}
            <motion.div
              className="relative bg-gradient-to-br from-white/95 via-white/90 to-indigo-50/30 backdrop-blur-xl rounded-3xl p-8 lg:p-12 border-2 border-black shadow-2xl"
              initial={{ y: 50, opacity: 0 }}
              animate={aboutInView ? { y: 0, opacity: 1 } : {}}
              transition={{ duration: 0.8, delay: 0.4, ease: 'easeOut' }}
            >
              <div className="grid lg:grid-cols-2 gap-12">
                <div className="space-y-6">
                  {[
                    { label: 'Artist', value: 'Sammy' },
                    { label: 'Project', value: 'Lazy Perfectionist' },
                    { label: 'Location', value: 'Hong Kong' },
                    { label: 'Genre', value: 'Instrumental Progressive' },
                  ].map((item, i) => (
                    <motion.div
                      key={i}
                      className="flex items-center justify-between py-3 border-b border-black/20 last:border-0"
                      initial={{ x: -30, opacity: 0 }}
                      animate={aboutInView ? { x: 0, opacity: 1 } : {}}
                      transition={{ delay: 0.5 + i * 0.1, ease: 'easeOut' }}
                    >
                      <span className="text-black/70 font-medium">{item.label}</span>
                      <span className={`font-semibold ${
                      item.label === 'Genre' ? 'text-indigo-600' :
                      item.label === 'Artist' ? 'text-slate-700' :
                      'text-black'
                    }`}>{item.value}</span>
                    </motion.div>
                  ))}
                </div>

                <div className="space-y-6">
                  <motion.p
                    className="text-lg text-black/90 leading-relaxed"
                    initial={{ x: 30, opacity: 0 }}
                    animate={aboutInView ? { x: 0, opacity: 1 } : {}}
                    transition={{ duration: 0.6, delay: 0.7, ease: 'easeOut' }}
                  >
                    Lazy Perfectionist is an instrumental progressive rock/metal project that blends technical precision with emotional storytelling. From a Hong Kong bedroom, orchestral layers meet EDM-inspired synths and anime music influences.
                  </motion.p>

                  <motion.blockquote
                    className="text-lg text-indigo-600 italic font-dm-serif border-l-4 border-indigo-500/30 pl-6 bg-gradient-to-r from-indigo-500/5 to-transparent"
                    initial={{ x: 30, opacity: 0 }}
                    animate={aboutInView ? { x: 0, opacity: 1 } : {}}
                    transition={{ duration: 0.6, delay: 0.9, ease: 'easeOut' }}
                  >
                    "Flaws are the heartbeat of art. Instrumental music is a universal story. Commit when it captures intent."
                  </motion.blockquote>
                </div>
              </div>

              <motion.div
                className="mt-12 pt-8 border-t border-black/20"
                initial={{ opacity: 0 }}
                animate={aboutInView ? { opacity: 1 } : {}}
                transition={{ duration: 0.6, delay: 1.1, ease: 'easeOut' }}
              >
                <div className="flex flex-wrap justify-center gap-6">
                  {[
                    { href: 'https://instagram.com/lazyperfectionist_official', label: 'Instagram' },
                    { href: 'https://tiktok.com/@lazyperfectionist_official', label: 'TikTok' },
                    { href: 'https://www.youtube.com/watch?v=Hw2a43RV1p0', label: 'YouTube' },
                  ].map((social, i) => (
                    <motion.a
                      key={social.label}
                      href={social.href}
                      target="_blank"
                      rel="noopener noreferrer"
                      className="bg-white/95 text-black px-6 py-3 rounded-xl font-medium border-2 border-black hover:border-indigo-500 hover:bg-indigo-50/50 transition-all duration-300 hover:shadow-lg hover:-translate-y-1"
                      whileHover={{ scale: 1.05 }}
                      whileTap={{ scale: 0.95 }}
                      initial={{ y: 20, opacity: 0 }}
                      animate={aboutInView ? { y: 0, opacity: 1 } : {}}
                      transition={{ delay: 1.3 + i * 0.1, ease: 'easeOut' }}
                    >
                      {social.label}
                    </motion.a>
                  ))}
                </div>
              </motion.div>
            </motion.div>
          </div>
        </div>
      </section>

      {/* -----------------------------------------------------------------
          Artist Timeline Section */}
      <ArtistTimeline />

      {/* -----------------------------------------------------------------
          Footer with Enhanced Design */}
      <footer className="relative bg-black/90 backdrop-blur-xl border-t border-white/10 overflow-hidden">
        <div className="absolute inset-0 opacity-10">
          <div className="absolute inset-0 bg-gradient-to-br from-indigo-500/20 via-transparent to-purple-500/20" />
        </div>

        <div className="relative max-w-7xl mx-auto px-6 sm:px-8 lg:px-12 py-16">
          <div className="grid md:grid-cols-3 gap-8 mb-12">
            <motion.div
              initial={{ y: 30, opacity: 0 }}
              whileInView={{ y: 0, opacity: 1 }}
              transition={{ duration: 0.6 }}
              viewport={{ once: true }}
            >
              <div className="mb-6">
                <img
                  src="/assets/img/logo.png"
                  alt="Lazy Perfectionist Logo"
                  className="w-16 h-16 rounded-2xl object-cover shadow-xl"
                />
              </div>
              <h3 className="text-2xl font-bold text-white mb-4 font-dm-serif">
                Lazy Perfectionist
              </h3>
              <p className="text-gray-400 leading-relaxed">
                Bedroom Instrumental Rock from Hong Kong. Creating music that moves the soul.
              </p>
            </motion.div>

            <motion.div
              initial={{ y: 30, opacity: 0 }}
              whileInView={{ y: 0, opacity: 1 }}
              transition={{ duration: 0.6, delay: 0.1 }}
              viewport={{ once: true }}
            >
              <h4 className="text-lg font-semibold text-white mb-6">Quick Links</h4>
              <div className="space-y-3">
                {['Home', 'Music', 'Blog'].map((item) => (
                  <Link
                    key={item}
                    href={item === 'Home' ? '/' : `/${item.toLowerCase()}`}
                    className="block text-gray-400 hover:text-indigo-400 transition-colors duration-300"
                  >
                    {item}
                  </Link>
                ))}
                <a
                  href="https://linktr.ee/lazyperfectionist_official"
                  target="_blank"
                  rel="noopener noreferrer"
                  className="block text-gray-400 hover:text-indigo-400 transition-colors duration-300"
                >
                  All Links
                </a>
              </div>
            </motion.div>

            <motion.div
              initial={{ y: 30, opacity: 0 }}
              whileInView={{ y: 0, opacity: 1 }}
              transition={{ duration: 0.6, delay: 0.2 }}
              viewport={{ once: true }}
            >
              <h4 className="text-lg font-semibold text-white mb-6">Connect</h4>
              <div className="space-y-3">
                <a
                  href="https://open.spotify.com/track/1XIv8JGEDU9MZT6HEFmdk8"
                  target="_blank"
                  rel="noopener noreferrer"
                  className="flex items-center gap-3 text-gray-400 hover:text-green-400 transition-colors duration-300"
                >
                  <Music className="w-4 h-4" />
                  Spotify
                </a>
                <a
                  href="https://medium.com/@lazyperfectist"
                  target="_blank"
                  rel="noopener noreferrer"
                  className="flex items-center gap-3 text-gray-400 hover:text-blue-400 transition-colors duration-300"
                >
                  <BookOpen className="w-4 h-4" />
                  Medium
                </a>
                <a
                  href="https://www.youtube.com/watch?v=Hw2a43RV1p0"
                  target="_blank"
                  rel="noopener noreferrer"
                  className="flex items-center gap-3 text-gray-400 hover:text-red-400 transition-colors duration-300"
                >
                  <Youtube className="w-4 h-4" />
                  YouTube
                </a>
              </div>
            </motion.div>
          </div>

          <div className="border-t border-white/10 mb-8" />

          <motion.div
            className="flex flex-col md:flex-row justify-between items-center"
            initial={{ opacity: 0 }}
            whileInView={{ opacity: 1 }}
            transition={{ duration: 0.6, delay: 0.3 }}
            viewport={{ once: true }}
          >
            <div className="text-gray-400 mb-4 md:mb-0">
              © 2025 Lazy Perfectionist. All rights reserved.
            </div>

            <motion.div className="flex items-center space-x-6" whileHover={{ scale: 1.05 }}>
              {[
                { href: 'https://open.spotify.com/track/1XIv8JGEDU9MZT6HEFmdk8', icon: Music, color: 'hover:text-green-400' },
                { href: 'https://medium.com/@lazyperfectist', icon: BookOpen, color: 'hover:text-blue-400' },
                { href: 'https://www.youtube.com/watch?v=Hw2a43RV1p0', icon: Youtube, color: 'hover:text-red-400' },
              ].map((social, i) => (
                <a
                  key={i}
                  href={social.href}
                  target="_blank"
                  rel="noopener noreferrer"
                  className={`text-gray-400 transition-colors duration-300 ${social.color}`}
                  aria-label={social.icon.name}
                >
                  <social.icon className="w-5 h-5" />
                </a>
              ))}
            </motion.div>
          </motion.div>
        </div>
      </footer>
    </div>
  );
}